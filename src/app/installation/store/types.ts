
// ---- Installation ----
export interface IInstallation {
  selectedType?: string;
  selectedDbFolder?: number;
  selectedDbStream?: number;
  selectedDataApp?: number;
  nilm?: number;
  rootFolderId?: number;
  refreshing?: boolean;
  busy?: boolean;
}
